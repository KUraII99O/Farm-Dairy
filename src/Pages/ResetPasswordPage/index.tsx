import React from 'react';
import ResetPassword from '../../component/RestPassword';

const RestPassword = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <ResetPassword />
    </div>
  );
};

export default RestPassword;
